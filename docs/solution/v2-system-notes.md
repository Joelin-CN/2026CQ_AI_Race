# v2 赛题系统实测行为笔记（2026-09-14）

> 记录对 2026.09.12 补丁版赛题系统（release_v2）的实测行为——全部来自 train/test
> 模式一手观察，官方文档未写明。**自定义 agent 的行为必须匹配这些语义。**
> 后续新发现继续追加到本文。

## 1. 版本与校验事件时间线

| 时间 | 事件 |
|---|---|
| 09-13 02:28 | 下载网盘 release.zip（MD5 `1876f072…`，后被证实为旧构建） |
| 09-13 深夜 | 首次打包上传被官网拒收："答题文件校验未通过" |
| 09-13 23:55 | 重新下载 release.zip（MD5 `cca6cde4…`，3 文件补丁：arena_offline.exe / arena_resources.pack / tongsim_server.exe，即"更新了赛题题库"） |
| 09-14 08:06 | 再次下载核对，MD5 不变 |
| 09-14 上午 | 技术支持确认：新版服务端校验 09-14 中午 12:00 生效（此前拒收是服务端未切换） |

经验：结果文件头部 `ARENA_RESULT_V4c91d6869a96a2bc4…` 新旧相同（格式+密钥常量），
官网校验的是**结果内容里的题库标识**；网盘同版本号会原地换文件，**每次提交前
重比 MD5**，被拒先怀疑版本、再问答疑群。

## 2. v2 核心语义（与 v1 的差异）

1. **一题一提交（one-shot）**：问答题（raven/counting/npc）提交一次即结束该题，
   无论对错，**没有重试机会**。v1 时代"答错继续试"的循环不再存在。
   → 策略含义：首选答案的正确率就是一切；基线 counting 的 10/10 全对含重试修正，
   在 v2 下不可复现，确定性计数更加重要。
   （注：raven 已实测确认；counting/npc 待复核——若仍有重试，行为会不同。）
2. **一题一连接**：agent 答完自己的题，其 session 立即 FINISHED(199)。系统推进到
   下一题后等待**新的 agent 连接**。官方 builder 的 `--run_times N` 循环天然匹配；
   自定义 `run()` 里常驻轮询 session 状态会永久停在 RUNNING(101) 并把系统拖入
   等待死锁。
3. train 与 test 行为一致（都遵循上述两条）；test 额外隐藏赛题、结果加密。

## 3. 判卷与归档行为

- 归档位置：`<release>/arena_offline/eval_res*.json`（train 明文；test 不落明文）。
- **答对时**：归档含 `correct_answer`（这就是我们的标注来源）。
- **答错时**：`correct_answer: null`（v2 起防漏题，不再泄露答案）。
- 每次新结果写入前，旧 `eval_res.json` 被重命名为 `eval_res.<时间戳>.json`。
- SessionStatus：101=RUNNING、199=FINISHED、99=TERMINATED（agent 视角，题答完即 199）。
- 任务系统日志（start_test 控制台）是 train 模式下最全的信息源。

## 4. 打包与运维坑

- `package-results --package-results-dir .` 的**基准目录是 `arena_offline`**，
  bins 就在其中时参数传 `.`；传错会报"result directory does not exist"。
- 换任务：杀 `arena_offline.exe` + `tongsim_server.exe` → 重跑 `start_test.bat`；
  UE 客户端全程不用重启。无 agent 连接时客户端显示空白天空盒属正常待机。
- Agent 进程出错/挂死后必须重启赛题系统，否则后续结果不可用。
- `uv run arenaagent` 若报 `No module named 'arena'`：重跑 pb2 生成脚本后
  `uv pip install --no-deps -e official_source/gitee/baseline-agent` 刷新 editable。

## 5. raven 任务的可用解法（已验证）

- 题面：一张 2376×1200 大图，横排 3 道图形推理（3×3 缺角 + 8 候选，候选按行
  编号 1-8），答案=三题编号连成三位数。
- 官方 `solve_raven`（ResNet18+MLP）在 v2 题库上**首选命中**：train 实测 13/13
  （6 种题型），秒级提交 97.0~99.7 分。
- v1 上 0/10 全错是旧版判卷/题库缺陷（同一套模型与裁剪代码，仅换系统即全对）。
- 实现与运行见 `src/cqairace/raven_proto.py`：
  `uv run python -m cqairace.raven_proto --run_times 10`
  （需带 VLM_CLIENT_* 环境变量以通过官方 client 构建断言，实际不调用模型）。

## 6. 素材库索引

| 路径 | 内容 |
|---|---|
| `temp/p2_raven/collected/` | 16 张唯一题图（彩色原图）+ 裁剪子图目录 |
| `temp/p2_raven/trace.jsonl` | 全部提交轨迹（题图哈希/名次/答案） |
| `temp/p2_counting/trace.jsonl` | counting 提交轨迹 |
| `temp/p4_tidyroom/trace.jsonl` | tidyroom 每件 take/put 轨迹 |
| `temp/baseline_records/` | 各任务 eval 归档 + 运行日志 + 历次提交包 |
| `temp/pdf_images/` | 官方 PDF 中提取的评分公式图 |

## 7. tidyroom 实测语义（2026-09-14，自研 agent train 实跑 12 轮）

1. **"can not take this object for not pickup" 是服务端本地判断**：
   不触发导航、0.0s 返回——对不可抓物体（人脚上的鞋等）失败重试的代价
   几乎为零，"失败即拉黑"策略零成本；据此把物品候选尺寸上限放宽到 80cm
   （大件失败免费，成功一件赚一件）。
2. **train 每轮随机布局**：物品在 room_space 边界内随机撒地面，物体感知
   ID 每轮不同；**任何按 ID/坐标硬编码的策略在 test 必挂**。
3. **判卷机制（逆向 arena_offline.exe / Nuitka 字符串证实）**：
   - 出题：`tidyroom.json` 定义 9 类物品清单 + 容器（3 沙发/6 桌/冰箱/
     垃圾桶/鞋柜），物品随机 spawn 且**生成器保证初始全错**（放对即重丢）；
   - 判卷："放对" = 物体 **world_aabb 中心** 落入**任一**合法容器 bbox
     （container_names 是列表，多容器全合法）；判卷读**最终静止位置**；
   - 计分：0.8×correct/total + 时间分，超时时间分归零。
4. **放置的物理模型（实机观察，run11）**：`move_and_put_down` 的 move 点
   **不控制人物朝向**，到达后按行进方向放手，物体释放在"人物面前"——
   没正对容器就全掉在旁边地上；而 **`put_down_sth` 是强制坐标放置**
   （物体直接出现在指定坐标，可穿模）。正确组合 =
   `move_to_object(容器)`（面向）+ `put_down_sth(容器AABB内部点,
   force_locate=True)`（强制入体）→ run12 实测 5/5 全计分，48 分。
5. **deepseek-flash 视觉大 prompt（30+ 物体元数据+复合图）单次推理
   ~68s**：串行多帧分类必超时；v2 改为流水线（扫描逐帧即时异步发 VLM、
   规则分类先行搬运、VLM 后到只补充），VLM 等待与走路时间重叠。
   模型还不守输出格式约定（返回 类别→ID列表 而非 ID→类别），解析器
   必须两种都兼容。**VLM 帧线程内 invoke 不得走 tongsim 超时池**
   （2 worker 会被长占导致转身/感知排队超时，run9 教训）。
6. **确认逻辑必须复刻判卷几何**（bbox 中心 ∈ 容器 bbox）：v2 曾用
   place_location 锚点+容差判定，放置在茶几顶的杯子"锚点通过"而
   "bbox 中心超界判错"，两轮确认全过却 0 分。
7. **UE 客户端被前台全屏程序（游戏）占用时感知退化**：可见物体从 52
   跌到 3，spawn 返回空 ID；重启 tongsim/arena 均无效，必须释放前台
   GPU 占用。UE 长跑（10+ 轮）也会劣化（turn 超时/感知失败），跑验证
   前确认机器空闲 + 定期重启 UE（重启 UE 后必须连带重启 tongsim/arena）。
8. **4×90° 原地扫描已覆盖全场**（二轮扫描实测新增可搬物品 0），物品
   分布两簇：客厅茶几周围地毯区 + 玄关鞋区，全部贴地；单房间无门
   （多房间+开门是决赛 FinalEscapeRoomTask 结构）。
9. **不可移动物清单（枚举定案，2026-09-15）**：train 静态题的 4 只鞋
   （白鞋+红靴）与矩形枕 13 全部 `not pickup`（pickup 白名单硬设计，
   与姿势/距离/换手无关）。**`put_down_sth(force_locate)` 是嵌入式传送，
   不产生物理碰撞推力**——想用"砸重物挤走不可抓物"的路线不通
   （59×75cm 枕旁连砸 6 件，坐标零位移）。
10. **UE 劣化规律**：重启 UE 后第 1 轮感知正常、第 2 轮即退化（52→3 物体）
    的模式反复出现；对物理异常物体（z=-10 墙角标记）执行 take 疑似加速
    损坏。敏感实验前重启全栈；感知物体数是健康度指标（<10 即废）。
11. **tongsim client 接口地雷**：`look_at_object` / `point_at_object` /
    legacy 容器接口（move_and_put_down_object_in_container）会关闭 client
    的 event loop，之后所有导航类长 RPC 报 "Event loop is closed"——生产
    agent 只用 take/put/move_to_object/turn/感知五个接口。
12. **tidyroom 计分结构（train 静态题实测反推）**：可移动物 5 件全对
    =80.88 分；分差到 100 全部来自时间分（run14 总耗时 ~175s），完成度
    无可再得之物（不可移动物枚举穷尽）。时间分系数非简单 0.2×线性
    （80.88 无法用 0.8/0.2 线性拟合），精确曲线仍待 test 样本。
13. **容器/物体感知 ID 跨轮不稳定，但房间几何稳定（2026-09-15 深夜
    R9 复盘定案）**：同一件家具在不同轮拿到不同 ID——16 在俯视图 v3
    那轮是餐边柜、在 R9 是**餐桌**；47 在 R9 是鞋柜、在 R10 是不可抓鞋。
    而几何完全稳定：table 15（茶几）放点 R9/R10 逐毫米一致
    (555.6,323.9)。**任何场景先验必须以世界坐标为锚，绝不能以 ID 为锚**
    （`annotated_topview_v3.jpg` 的 ID 标签只在制作它的那一轮有效）。
    区域方位（几何锚）：西侧=餐区+厨房（餐桌/餐边柜/餐边高柜/橱柜/冰箱），
    东南=玄关鞋区（窄深贴墙鞋柜），中部+东侧=客厅（L 沙发/双人沙发/
    茶几/电视柜）。
14. **R9 复盘（test，2026-09-15 11:39，bin 已入 09-15pm 提交包）**：
    6 件放置经逐帧图像核验**全部合法**——37 杯+36 食→茶几 15、34 垃圾→
    桶 18、35+32 鞋→鞋柜 47、33（深褐粗短马克杯，vision 核验）→**餐桌
    16**（confirm_33 帧可见长桌+餐椅，非餐边柜）。若 total=可搬件数
    （train 5/5=80.88 佐证），R9 完成度=6/6 满完成度，**预估 80+时间分**
    （用时 116s vs train 175s 得 0.88）。待 09-16 9:30 官网分校准。
15. **分割面板同一编号会多次出现**（大件家具被拆成多个子网格、每块
    都印号；R9 帧0 中 3/9/10/17 各出现 2-3 次）——OCR/统计时不能以
    "编号唯一"做约束；09-14 晚 run2 杯×3+食放 (-265,497,z≈81) 即
    餐边高柜（z=半高≈81→柜高≈162cm，远超桌面 75cm），**疑似当时就
    放错容器**（未计分验证，仅几何推断）。
16. **容器解析的体积顶替隐患（Phase 2 加固候选）**：`_resolve_containers`
    对 VLM 投票容器取"同类体积最大者"，累积投票可在轮中把 table 从
    茶几翻到大体积餐边柜/高柜（R9 的 37→15 但 33→16 同类不同容器即是
    指纹；本次翻到的是合法餐桌未失分）。加固方向：给 table 加与
    shoe_cabinet 同款的规则校正（VLM 选的 table 不满足 brown+rect+
    25≤dz≤55+宽<200 则用规则结果），或同类别首件放置成功后锁定容器。
17. **感知复合图编号是固定像素渲染（R11 实测，2026-09-15）**：请求
    2560×720 原生分辨率后数字编号仍为 ~8-10px 黑实心（描边观感来自
    缩放），**不随分辨率放大**——高分辨率请求对编号 OCR 无增益，只让
    面板更清晰。行车记录仪已改存全分辨率帧（景物更清楚，复盘友好）；
    编号 OCR 在 8-10px 下模板匹配只能稳定区分部分数字（6/8/9、1/4
    互混），生产复盘用 vision 整组读数兜底（temp 脚本
    final_r11_figure.py：物品区数字戳 ×6 放大 → 一次 vision 整组读数
    → 命中物品清单才画框，R11 验证 5/5 框锚定正确编号）。
18. **静态容器先验（2026-09-15 决策采纳，队友方案对照后取长）**：
    test/train 同一 UE 地图、仅物品随机撒、**容器几何跨轮逐毫米稳定**
    （§7-13）。R11 逐帧 AABB 实测 + 队友核验包坐标 + R9/R10 放点
    三方互证定案（单位 cm，xy=AABB 中心）：

    | 容器 | 坐标 (x,y) | 实测 AABB | 证据 |
    |---|---|---|---|
    | 茶几 table | (577, 365) | 72×137×29, z16-45 | R11 id15=队友先验=R9/R10 放点(555.6,323.9)∈AABB |
    | 主沙发 sofa | (282, 374) | 112×410×99, z0-99 | R11 id14=队友先验逐位一致 |
    | 垃圾桶 trash_bin | (814, 267) | 27×27×35, z4-39 | R11 id18=队友先验=R9 放点(805.8,258.7) |
    | 玄关鞋柜 shoe_cabinet | (780, -144) | 109×23×60, z3-63 | R11 id46=R9 放点(746.8/779.6,-150.9)∈AABB |

    实现为"先验锚定"而非硬编码直放：①`_rule_container` 规则命中者中
    取先验附近者（替代纯体积最大，容差外命中留作换地图回退）
    ②`_resolve_containers` 对 VLM 投票结果的远偏选择（>容差）做先验
    校正——根治 R9 的体积顶替翻转（§7-16）③`_prior_container` 按尺寸
    盒+坐标在规则/VLM 全失效时兜底（color=Unknown 也能找到）。
    12 个单测覆盖（tests/test_tidyroom_containers.py）。实机 A/B 待下
    次起栈验证。**注意：先验不含餐桌(-217,521)——cup 判卷多桌全合法，
    锚定茶几即可且离物品簇更近（走位更短）。**
    附带发现（待办）：R11 的 36 号黑色枕 shape 元数据=pillow、56 号
    gray box 在高处——现行规则无 pillow/box 分支，VLM 全挂轮会漏分类
    （shape 覆盖缺口，Phase 2 候补）。
19. **分割面板色块是"随机实例色"，与物体真实颜色无关（2026-09-15
    视觉模型核验 R11 存档帧 frame_scan_r1_0.jpg 定案）**：米白地板渲染
    为粉色块、天花板淡紫、黑色垃圾桶=青绿块（id18）、人物=蓝紫块（id6）。
    含义：①"用元数据 color 直接匹配色块做免读号绑定"的路线判死刑；
    ②dashcam 洪泛色块（loDiff/upDiff=18）仍有效——它只依赖区域内颜色
    一致性，不依赖颜色语义；③远帧 VLM 票的绑号只能靠读数字戳或结构性
    绑定（见 20）。
20. **绑号三路线与识别管线 v4 决策（2026-09-15 讨论定案）**：object_id
    与右面板数字的绑定只有三条路——a) 模板 OCR（8-10px 下 6/8/9、1/4
    互混，只能作加速不能作主路径）；b) VLM 转写放大数字戳（R11 离线
    5/5 验证，主路径）；c) 到场结构性绑定（走到已知 oid 跟前，所见即
    该物，无需读号——搬运本来就要走到物品跟前，走路零额外成本）。
    识别管线 v4 = 三段式 gate：扫描 → 基线识别（截定位→VLM转写→洪泛
    区域→裁剪自标编号→轻量 prompt 分类，像素面积加权投票，截止线
    ~100s）→ 分类锁定 → 纯执行 + 歧义件到场确认（特写一票权重 5）。
    治三病：VLM 读错小号绑错 oid、远景近视、放错不可逆（_done_oids
    后票即弃）。参数（截止线/权重公式/歧义阈值）待离线实验定稿。
21. **v4 离线实验定案（2026-09-15，temp/p4_tidyroom/v4_probe.py，
    R11 存档帧回放，结果 v4_probe_results.json）**：
    - **时延瓶颈是思考模式而非 prompt/图大小**：deepseek-flash 思考关
      时拼格分类 0.8~1.0s、单件特写 0.5~0.8s、戳转写 0.7~2.0s（各 5
      次实测）；思考开时旧生产大 prompt 42~43s 且 8192 token 全被
      reasoning 吃掉正文为空（生产 68s/空文本/解析失败同类病理），
      轻量拼格思考开也要 10.8s 全 reasoning 正文空。**官方 openai
      client（client.py `_invoke` 只传 model+messages）无
      thinking/max_tokens 控制** → v4 感知调用改走自研直连客户端
      （思考关，读同一套 VLM_CLIENT_CFG_* 环境变量，官方客户端回退）。
      空文本失败类（R4/R5/R11 换帧重发的根源）随之消灭。
    - **多图一消息 API 支持但质量差**（content 数组多 image_url 返回
      全 other）→ 弃用，统一用拼格。
    - **绑号准确率**（r1_0，25 戳组）：候选 6 件绑上 4 件（32/33/34/13），
      36 号黑枕戳被误读（绑号缺失→无票）；入口静态鞋 47-50 的戳仅 6px
      高，转写全 X 不可读——绑号缺失由规则（shape+成对启发）与到场
      确认（结构性绑定，无需读号）兜底，安全网成立。
    - **视觉准确率**（远帧裁剪）：33 杯 ✓/13 枕 ✓，32 鞋→枕 ✗、34
      食→垃圾 ✗——均为远距小裁剪（低权重票），印证加权投票+到场确认
      必要性；近距大区域件（33）一次即对。
    - **架构影响**：识别阶段 4 帧×2 调用并行 ≈ 3~5s（gate 截止线可从
      100s 收紧到 ~45s），到场确认 VLM 成本 ≈1s/件。时间分大幅改善
      空间打开，"用时间分买完成度"的代价几乎消失。
22. **v4 新旧管线回放对比与判定细化（2026-09-15，temp/p4_tidyroom/
    v4_replay.py，R11 四帧 ×3 次）**：
    - **核心安全性质**：v4 判定细化后，识别错误一律退化为"歧义→到场
      确认"，不再产生"错类别→错容器"的放置。回放首跑曾暴露：远距
      单帧低权票（w≈1.0，无竞争票）直接定案把 32 号鞋判成枕——修正
      为**单帧低权票不定案**（须 ≥2 帧同票，或单帧 w≥1.5 即近距大
      区域特写；到场确认票 w=5 满足）。
    - 整帧 v3 管线在同一回放里 VLM 对真值件绑定贡献为 0（三次全空，
      靠规则撑住 3/5）——VLM 读小号的产出方差极大，时灵时不灵；
      v4 的价值在确定性与可测量性（绑号/视觉错误分离统计），
      单次回放准确率与 v3 相当。
    - 其他细化：已解析容器（垃圾桶 18 尺寸可过物品筛选）不参与定类
      与到场确认；直连客户端加 temperature=0（转写跨运行方差大）；
      规则补 shape=pillow（R11 的 36 号黑枕在回放中即由该分支命中）
      与"非黑小 box→trash"。
    - 实施后状态：32 单测全绿（tests/test_tidyroom_v4.py 20 +
      test_tidyroom_containers.py 12）；实机 A/B 待起栈（与容器先验
      同场，含 TIDYROOM_V4=0 对照）。
23. **v4 实机四轮（2026-09-16 凌晨，train，每轮全栈重启，存档
    test_sessions/*train_v4*）：80.1 / 81.42 / 81.03 / 81.27——全 5/5
    满完成度，全部 ≥ 旧纪录 80.88，81.42 为 train 历史新高**。
    规则+到场确认每轮扛住（41→pillow 确认 8.6~8.9s/轮稳定跑通）；
    基线层 VLM 在第四轮（v4d）达成实机贡献：votes={36:trash(1.0),
    34:cup(1.0), 13:pillow(1.99=两帧加权), 41:pillow(确认 5.0)}，
    歧义件清零。前三轮基线层零贡献，实机抓出三个连环 bug（每轮一个）：
    ① v4 首轮：读号链返空回退**旧整帧路径走官方客户端（思考开 42~68s
    +reasoning 吃满正文空）**，烧穿 45s gate——修复=回退也走直连
    （gate 立降至 ~4s）；② v4b：转写返回**未加引号的裸值 X**
    （`"17":X`，非法 JSON 整体炸掉→reads=0；是否引号随机，这就是
    历轮"时灵时不灵"的真相）——修复=_extract_json 裸值补引号；③ v4c：
    分类返回**中文弯引号**（`{“13”:“pillow”}`，中文 prompt 诱发）——
    修复=弯引号归一+尾逗号清理+分类空结果重试一次。三个都是 deepseek
    JSON 格式伤家族成员，与既有"类别→ID 反向输出"同类——轻量 prompt
    输出短，格式伤概率反而放大，解析器必须全兜（34 单测含全部伤型）。
    入口静态鞋帧 pairs=0 属预期（6px 戳不可读，靠规则+确认兜底）。
    时间分观察：~100s 轮 81.0~81.4 vs 127s 轮 80.1，仍非线性，
    待 09-16 官网分校准。**待办：test 实机 A/B（含 TIDYROOM_V4=0
    对照）+ 提交包更新。**

## 8. VLM 输出格式伤排查清单（2026-09-16 立档，全任务适用）

> 出处：tidyroom v4 实机四轮连环抓现行（§7-23）+ 历轮旧账。**任何任务
> 出现"VLM 解析失败/空文本/时灵时不灵"，先按下表对症状，再看别的。**

| # | 症状（日志关键字） | 根因 | 修复/规避 | 状态 |
|---|---|---|---|---|
| 1 | `Expecting value: line 1 column 1 (char 0)` 且耗时 40~70s | **思考模式吃满 token**：官方 client 只传 model+messages，flash 默认思考（effort=high），reasoning_content 与正文分离，正文为空 | 走 `vlm_direct`（thinking disabled + temperature=0，0.5~1s）；官方客户端仅作直连不可用时的兜底 | ✅ 已修 |
| 2 | VLM 空文本反复出现、换帧重发频繁 | 同 #1（R4/R5/R11 的"间歇全挂"本质） | 同 #1 | ✅ 已修 |
| 3 | 解析失败但肉眼检查返回"看着正常" | **裸值未加引号**：`"17":X`（X/单词不加引号，是否出现随机——"时灵时不灵"的另一真相） | `_extract_json` 裸值补引号重试（stamp_perceive.py） | ✅ 已修 |
| 4 | 解析失败且返回含中文引号 | **弯引号**：`{“13”:“pillow”}`（中文 prompt 诱发） | 同上：弯引号归一 + 尾逗号清理 | ✅ 已修 |
| 5 | 解析"成功"但语义对不上 | **反向映射**：返回 类别→ID 列表 而非 ID→类别 | `_normalize_map` 双向兼容（tidyroom_agent.py） | ✅ 已修 |
| 6 | 有裁剪但分类空（diag cats=0） | #3/#4 随机命中分类调用 | 分类空结果同 prompt 重试一次（~1s） | ✅ 已修 |
| 7 | 首字符不是 `{` / 前置思考文本 | 模型输出前缀废话 | prompt 内钉死"第一个字符必须是{" + 尾部正则取 `{...}` | ✅ 既有 |

**排查入口**：
- 生产日志 diag 逐环节定位：`v4 读号链空结果 diag={'groups':G,'reads':R,'pairs':P,'crops':C,'cats':K}`——G=0 戳定位问题、R=0 转写/解析问题（本表 #1~#4）、P=0 绑号不命中（戳不可读/转写错号）、C/K=0 分类问题（#3~#6）。
- 离线重放：`temp/p4_tidyroom/v4_probe.py`（素材/时延/准确率）与 `v4_replay.py`（新旧管线对比），用存档帧即可复现，零实机成本。
- 单测防回归：`tests/test_tidyroom_v4.py` 含全部伤型用例（34 绿）。
24. **容器全图（2026-09-16 立档，train_v4d × R11 双场景逐 0.1cm 互证；
    后续轮次可直接按坐标核对/走到）**。判卷 json 称 3 沙发/6 桌/冰箱/
    垃圾桶/鞋柜——第一人称视野内可见并双场景确认的如下（xy=AABB
    中心，cm；z 为 AABB 范围；两场景同 id 同几何，含 8/11/12/13/14/
    15/16/17/21-24/43-46/52 全部一致）：

    | 容器/家具 | id | 坐标 (x,y) | 尺寸 dx×dy×dz | z 范围 | 判卷地位 |
    |---|---|---|---|---|---|
    | 茶几（主桌） | 15 | (577.2, 365.0) | 72×137×29 | 16~45 | ✅ 合法 table（R9/R11 放点实测计分）|
    | 餐桌（餐区） | 16 | (-217.0, 521.0) | 160×80×76 | 3~79 | ✅ 合法 table（R9 33 号放此计分）|
    | 餐边柜 | 8 | (-327.9, 242.7) | 75×207×84 | 0~83 | ⚠️ 84cm 高于常规桌面，是否算 table 未验证 |
    | 主沙发 | 14 | (282.5, 374.3) | 112×410×99 | 0~99 | ✅ 合法 sofa（L 形，410 长）|
    | 白色矮柜 | 12 | (541.9, 584.8) | 112×90×84 | -1~84 | ❓ 疑似脚凳/单沙发，类别未验证 |
    | 垃圾桶 | 18 | (814.0, 267.0) | 27×27×35 | 4~39 | ✅ 合法 trash_bin（§7-18）|
    | 玄关鞋柜 | 46 | (779.6, -143.9) | 109×23×60 | 3~63 | ✅ 合法 shoe_cabinet（§7-18）|
    | **冰箱** | 7 | (112.3, 113.6) | 29×51×160 | 2~162 | ❓ 判卷容器清单含冰箱；对哪类物品合法未验证（推断 food）|
    | 厨房低台 | 17 | (813.9, 412.8) | 42×241×37 | 0~37 | ❓ 37cm 低台，R5 曾误选灶台丢分，勿主动使用 |
    | 玄关高柜 | 40 | (494.2, -124.0) | 114×28×171 | 4~171 | ❓ 窄高柜（鞋柜二号/衣帽柜），非必要不使用 |

    其余 43/44/45/52 为白色墙板/门（薄而高，非容器）；21-24 餐椅；
    39/40 一带为玄关储物区。"6 桌/3 沙发"中超出视野的部分无法穷举，
    需要时用 TIDYROOM_SCOUT 侦察点补扫。**放点策略不变：锚定
    15/16/14/18/46 五个已验证容器（走位最短），全图作为感知失效时
    的坐标核对与人工运维参考。**
    另：判卷"一容器只能放一件"的猜想已被 train 四轮证伪——三轮 32/
    34/35 同上茶几 15 仍 5/5 满完成度（若限一件完成度≤3/5、得分
    ≤48.2，与 80+ 实测矛盾）。~80 天花板=满完成度封顶，剩余全是
    时间分（非单调谜题，待 09-16 官网分校准）。
25. **队友核验包 98.11 分机制解构（2026-09-16 分析，源
    E:\临时\20260915\小组核验包\03_代码与脚本\preliminary_baseline_agent.py
    + 02_可读记录\整理房间实验记录.md 12 轮表）**：
    - **98.11 = 80(5/5 完成度) + 18.11 时间分，全轮 ~38s**。速度四来源：
      ①单帧感知（初始朝向一帧拿全候选，重试至 ≥20 物体；无 4×90° 扫描）
      ②零 VLM（纯 shape/color 尺寸规则分类）③**put_down_sth(force_locate)
      远距直放，全流程不走到容器**（垃圾/杯/食/枕全部传送入位，5 件只花
      5×"走到物品+put"的时间）④动作直发 tongsim、绕过外层任务循环的
      状态 RPC（注释称每动作省 ~12s；我们的 agent 同样直发，此项持平）。
    - **12 轮探针的宝贵副产品**：train 真值逐件确认——茶几三件
      （2 杯+苹果）+36 号核桃（食物→茶几）+抱枕→主沙发=5 件；
      **垃圾桶不注册为可交互容器**（指 `move_and_put_down_object_
      in_container` 类容器 RPC 报 no container found——接口层未注册，
      非物理/判卷层；坐标放置不受影响，我们从 v3 起即 put_down_sth
      force_locate 坐标直放入桶，R9 与 train 四轮均计分）；
      抱枕固定 Z=62 嵌入沙发碰撞体 0 计分，须 AABB.max.z+半高+4cm
      （与我们 §7-6 判卷几何教训同源）。
    - **交叉验证追加（2026-09-16 与我方四轮数据互证）**：36 号即
      我方规则判为 trash 的棕色不规则小物（shape=irregular→trash）
      ——"垃圾"与"核桃"是同一件物品的两个名字。同一件**放垃圾桶
      （我方四轮 5/5）与放茶几（其单项探针 16.0=1/5）均计分**——
      判卷给该物品的合法容器同时含桶与桌（container_names 多容器
      全合法的直接实证；语义"错分类"在合法容器重叠时零代价）。
      train 静态场景 id 跨会话稳定（我方四轮与对方 12 轮的 36 号
      为同一资产 BP_Food_Walnut_03）。
    - **时间分拟合仍失败**：他们自己的 53s 轮=48 分（时间分≈0）与
      38s 轮=+18.11 无法用任何单调公式统一；我们 100~127s 轮一律
      +0.1~1.4。经验阶梯：≤40s→+18，~53s→+0，100s+→+0~1.4——
      时间分大头压在极短区间，且计时起点疑似含轮次创建/agent 接入
      开销（待官网分校准）。
    - **他们的短板=我们的长板**：正式 test 轮只识别整理了 2 个杯子
      （单帧视野+几何过滤在 test 双物品簇/多形态下漏物）；我们 v4 在
      test R9 是 6/6。**合并策略：保 v4 感知识别，移植其提速点**——
      砍 move_to_object(容器) 走位（force_locate 远距直放已被其 98 分
      实证）+ confirm 感知降频 + 起跑自动化。
26. **放置链 v5 实机实验（2026-09-16 凌晨，train 三轮）**：移植队友
    98 分方案的核心提速点——`put_down_sth(force_locate)` 远距直放，
    砍掉 v3 的 `move_to_object(容器)` 走位（失败再走近兜底）。
    结果：**分数无大变化（81.3 / 81.42，持平 v4 最佳），总时长
    ~127s→~80s（-37%）**。单件 9.3~11.9s（v4 为 10.2~15.3s）——
    容器走位在 train 本来就短（茶几离物品簇近），每件只省 1~3s，
    大头仍是"走到物品+confirm 感知"。结论：**~18 分的时间分窗口
    （队友 38s 轮 +18.11）无法靠 80~127s 区间内的修剪触及**——我们
    四个 5/5 轮 80/84/100/105/127s 对应时间分 1.42/1.3/1.42/1.3/0.1，
    在此区间近乎不敏感。要摸到时间分须 <40s 级（砍扫描转身/砍 confirm/
    单帧感知/起跑自动化），或等 09-16 官网分校准出真实公式再定投入。
    附带三轮教训：①v5a 0 分=UE 慢加载（首帧 3 物体废帧当全世界），
    已加 `_wait_scene_ready` 就绪等待（<20 物体重试 8 次，队友同款）；
    ②二轮扫描在全部候选完成后仍白转（垃圾桶 18 号尺寸过物品筛选致
    `_all_candidates_done` 永假）——已修（容器不算候选）；
    ③v5c 单件黑名单（47-50/13 not pickup 秒拒）与到场确认（41→pillow
    6.0s）运转正常，识别层四轮稳定贡献票。
27. **test R12/R13（2026-09-16 凌晨，v4+v5 全栈首上 test）**：
    - R12：placed 4 / ~105s。读号链四帧 pairs=0（test 候选戳未命中，
      兜底链按设计工作：规则+整帧直连）；35 号错票（pillow 0.92 单帧
      低权）被"单帧低权票不定案"拦下→到场确认翻正为 cup ✓；40 号
      确认无结论→安全放弃（不盲放）。
    - **R12 复盘定案（用户行车记录仪核验）**：40 = 玄关盆栽（shape=
      plant，green，21×21×36 @(101,-127)），场景道具不可交互——
      元数据直接给了类型，新增 `_SCENE_PROP_SHAPES={"plant"}` 按
      shape 拉黑（id 跨轮不稳不可用），打通全部 7 个候选过滤点
      （候选/规则/成对/finalize/整帧 compact/容器先验/完成判定）。
    - R13：**placed 6 / 零放弃 / ~95s**——追平 R9 历史最佳且更快；
      两件歧义（36/37）全部到场确认翻正（trash/cup）后放置，盆栽
      零浪费（无确认往返）。bin 备份 test_round_13.bin，待复盘核验
      合法性后决定是否替换 R9。
28. **R12 定案补记（2026-09-16 用户复盘）**：58 号（gray box 56×44×32
    @(-27,146)）为**墙体构件**（焊死不可交互）——R12 实际 N=4，
    4/4 满完成度，预估 ~80.5-81。观察项：此类墙体盒有真实 3D AABB
    （非 z=-10 退化点、非薄板），若出现在扫描视野会走"候选→歧义→
    到场确认→无结论安全放弃"路径，浪费 ~9s/件；目前仅此一例
    （且不在扫描视野），复发再定拉黑规则（勿过拟合）。
29. **转写增强实验与误读更正（2026-09-16，temp/p4_tidyroom/
    v4_enhance.py，R12/R13 八帧）**：
    - **更正 §7-27 的错误结论**："test 读号链四帧 pairs=0"系误读——
      4 条告警全是玄关方向帧（r1_2/r1_3，groups 9/13 与离线完全吻合），
      且含换帧重发的重复 diag；异步日志乱序完成致误判全场。铁证在
      票权：R12 的 32(1.01)/35(0.92)/13(1.99 两帧)、R13 的 32(1.0)/13
      (1.99)——非整票权只有裁剪链能产生（洪泛面积），整帧回退恒 1.0。
      **基线层在两轮 test 均有贡献**。成功帧静默（无日志），失败帧
      告警——复盘时勿把告警当全貌。
    - **增强全无效**：×6/8/12 放大、CUBIC、二值化、锐化、定位下限
      放宽 6→4px，命中率一律 30~33%（10-11/33）——**画质不是瓶颈**，
      生产蒙太奇（×6 NEAREST）已足够。
    - **真实瓶颈是扫描距离**：客厅簇帧（2~4m）绑 3~5 件/帧（命中率
      50~70%），玄关簇帧（6~8m，戳缩至 4~6px）0 件。提升唯一真路=
      二轮扫描先走到玄关再转身扫（顺带开 submit_vlm）；收益取决于
      玄关簇出可搬物品的频率（R13 玄关全静态鞋，R9 曾有可搬鞋）。
30. **玄关近扫实测裁决 + 物品只刷客厅实证（2026-09-16，test R14/R15）**：
    - R14：placed 3（全枕→沙发）/ ~45s（最快轮），无歧义零确认；
      正常路径下二轮扫描永不触发（finalize 后候选必全部入队处理完，
      `_all_candidates_done` 恒真——二轮成为死代码，仅 FORCE 开关
      可达）。
    - R15（TIDYROOM_FORCE_ROUND2=1）：placed 6 零歧义（含两只可搬鞋
      规则直判进鞋柜）；强制玄关近扫实跑：站到鞋簇 0.5~1m 内再扫，
      **pairs 仍为 0**（groups 17/reads 8 全非候选）——**数字戳固定
      像素渲染实锤，走近不放大字号**，近扫对绑号无增益，该路线关闭
      （开关保留供复验）。扫描轮 2 新增物体 0。
    - **物品只刷客厅（用户假设，四轮实证）**：R12-R15 全部可搬物品
      出生位置 y∈[202,529]（客厅区），玄关（y<0）从未刷过可搬物品、
      只有静态干扰鞋。含义：扫描覆盖只需客厅方向；4×90° 可考虑减为
      客厅半圈 2 帧省 ~6s（低优先，待时间分公式定价值）。
    - bins：test_round_14.bin（3 件/45s，若 N=3 则为时间分最优候选）、
      test_round_15.bin（6 件/85s 含强制近扫，净耗时 ~62s）。R13/R14/
      R15 三个 bin 保留，待 9:30 官网分校准时间分公式后择优提交。
31. **五轮 test 连测稳定性报告（2026-09-16 02:25~02:43，R16-R20，
    每轮全栈重启，temp/run_test5.sh）**：

    | 轮 | placed | gave_up | 任务用时 | 歧义件 | 到场确认 |
    |---|---|---|---|---|---|
    | R16 | 4 | 0 | ~55s | 无 | 0 |
    | R17 | 4 | 0 | ~57s | 无 | 0 |
    | R18 | 3 | 0 | ~53s | 32/34 | 2（34→pillow、32→trash 全翻正）|
    | R19 | 4 | 0 | ~57s | 34 | 1（34→pillow）|
    | R20 | 5 | 0 | ~75s | 33/34/36 | 3（全翻正）|

    - **零失败**：gave_up=0、日志告警 0、无 UE 退化、无 VLM 挂——直连
      +思考关整夜稳定（对比 R11 时代 deepseek 间歇全挂）。
    - **placed 波动（3~6）源于场上物品数 N 的波动，非识别失败**：
      每轮可搬候选全部处理完毕（placed=各轮 N 候选全覆盖）。
    - **到场确认 6/6 翻正**（含 R20 三连），无误判。
    - 合并当晚 9 轮（R12-R20）：placed 4/6/3/6/4/4/3/4/5，均值 4.33，
      全部零放弃零异常。bins 全备份（test_round_12~20.bin）。
32. **漏件三病因与补盲扫修复（2026-09-16 凌晨，R17/R18 用户复盘
    发现 N=5 而 placed=4/3，推翻 §7-31"placed=N"结论）**：
    - **R17-33**（brown cylinder 10×10×1 贴地薄片）：min dim 1 < 
      `_MIN_DIM=2` 被 `_is_point` 误杀——墙角标记是 min=0 精确退化，
      阈值 2→1 安全（薄片=垫子/杯垫类）。
    - **R18-33**（white pillow 65×32×60 @茶几旁）：扫描帧只见局部
      →AABB 退化被滤；**确认帧看全了但感知结果没合并回 world**。
    - **R19-52**（brown sphere @ (604,469)）：出生点 4×90° 全程被
      家具遮挡，只在确认帧出现——**茶几/沙发西侧象限是扫描盲区**
      （三件漏件 @(436,192)/(591,277)/(604,469) 全在此象限）。
    - 修复三件套：`_MIN_DIM` 2→1；`_confirm_on_arrival` 感知合并进
      world（vol-max 同扫描）+ 主循环每件后 `_apply_rule_categories`
      +`_rebuild_queue`；二轮扫描改为**客厅西侧补盲扫**（走 (470,420)
      →4×90°→规则定类，玄关近扫实验代码移除）。
    - **验证（R21/R22）**：R21 补盲扫新增 7 物体→63 号确认 cup 上桌
      placed 6（+1 件）；R22 首轮仅 1 件→补盲扫挖出 57/58/59 三件
      全对 placed 4（+3 件）。补盲扫成本 ~14s/轮，换回 13 分/件，
      稳赚。R22 的 34 确认无结论放弃（复盘待看）。bins 21/22 已备份。
33. **pillow 尺寸界定与极小件兜底（2026-09-16，R22 用户复盘定策）**：
    - 界定依据（历史 9 轮普查）：真枕 max dim 全部 ≥38cm（最细颈枕
      45×16）；误判件全部 ≤15cm（R22-57 red drumstick 11cm 鸡腿、
      R14-33/R19-34 brown rock 10cm 石块、R18-34/R20-36 小 oval 9~15cm
      碎物）——空带 16~38cm，取下限 **30cm**。
    - 修复四件：①`_PILLOW_MIN_DIM=30`：规则层 pillow 分支（shape=
      pillow、beige/white rect）加下限；②确认层确定性守卫：VLM 确认
      pillow 但 max<30 → 改判 trash 进桶（不依赖 prompt 自觉）；③
      极小件兜底：无形状规则命中的 max<20cm → trash（用户定策：极小
      进垃圾桶；罐/果等形状规则在前不受影响）；④shape 词汇直判：
      rock→trash、drumstick→food(≥20)/trash(<20)；prompt C 加尺寸
      硬约束文案。
    - **R23 实机验证**：守卫两连击——34(16cm)/33(26cm) 被 VLM 确认为
      pillow 均被拦截改判 trash 进桶；placed 5、~65s、补盲扫 +4 物体
      无新件。33(26cm) 落空带偏大侧，真伪待复盘（frame_confirm_cls_
      33.jpg）。
    - **历史轮完成度修正**（本 bug 曾每轮偷走一件）：R14 实为 2/3、
      R18 实为 2/5、R19 实为 3/5、R20 实为 4/5。
34. **food 形态归类补充（2026-09-16，R23 用户复盘定案）**：历史规则
    food 全为 round 形（红圆 15~22cm 苹果 ×8、棕圆 3~7cm 核桃 ×4）；
    R23 新增两形态——33 **ring**（甜甜圈 31×32×11）/34 **slice**
    （切片 16×3×16）均为 food→茶几，曾被极小兜底误送桶。词汇直判
    补 `ring→food`、`slice→food`（先于极小兜底）。drumstick 维持
    ≥20→food / <20→trash（用户先前定策，桶对食物类有核桃双计分
    先例）。R23-35（brown/oval 8×8×8）用户未提出异议，维持极小
    兜底 trash。
35. **大件先验 + 行车记录仪拿起帧（2026-09-16，R24 用户复盘定策）**：
    - R24-33（gray/square 57×58×48）实为抱枕，被 VLM 确认成 trash 进
      桶——**历史可搬大件 max≥40cm 只有 pillow**（ring 31/靴 25/罐 34
      均在下）。新增 `_BIG_DIM=40`：无形状规则命中且 max≥40 → pillow
      （规则层）+ 确认层对称守卫（确认 trash 但 ≥40 → 改判 pillow）。
      小件定策维持（<20 无特征 → 垃圾桶）。
    - 行车记录仪修复：`frame_confirm_*` 是**放置后**拍的（物体已传送
      进容器，帧里自然看不见——R24 复盘发现）——新增 `_execute_one`
      拿起成功后立刻拍 `frame_took_{oid}.jpg` 近距帧（1280，~1.3s/件，
      仅 _SAVE_FRAMES 时），供人工核对"搬走的到底是什么"。
36. **大件先验污染修正（2026-09-16，R25 抓现行）**：大件先验首版
    （≥40 即 pillow）把 **29 个家具**（冰箱/沙发/餐桌/餐椅/墙板…）
    全标成 pillow（`_apply_rule_categories` 无尺寸上限）；该轮靠队列
    的 max≤80 过滤侥幸零损失（4 件放置全对），但 61×61×80 餐椅恰卡
    队列线。修正：①`chair` 加 `_SCENE_PROP_SHAPES`；②先验限定
    **40≤max<80 可搬区间**。R25 顺带验证：大件先验命中真件（33 号
    white/square 65cm→pillow→沙发 ✓）、`frame_took_*` 拿起帧四张全
    生成（记录仪修复生效）、补盲扫 +2 件（54 红苹果→桌、53 枕→沙发）。
    34 号 beige/cylindrical 17×26×9（新 shape 词"cylindrical"）确认
    trash 进桶，真伪待复盘 frame_took_34.jpg。
37. **今晚 test 收官（2026-09-16 04:04，R26）**：placed 3（35 彩色圆片
    12cm→桶/33 灰方块枕 57cm 大件先验直判→沙发/34 枕→沙发）、零歧义
    零确认、~75s；**cats 键数 9——家具污染清零**（R25 为 38），
    chair 拉黑+区间限定验证生效。今晚 R12-R26 共 15 轮 test：识别体系
    从三层兜底进化到 shape 词汇表+尺寸双向先验+双站位扫描+拿起帧全
    记录；bins 12~26 共 15 个备份，择优候选 R13（6/6 复盘验证）/
    R15/R21，待 9:30 官网分校准时间分公式后定提交。
38. **候选尺寸上限 80→100（2026-09-16，R26 用户复盘抓现行）**：R26-32
    白枕 81×55×37 从首帧扫描即在视野，但 `_MAX_ITEM_DIM=80` 三层卡死
    （规则分支/大件先验/队列入队全部 <80）——一厘米之差整件误挡。
    历史普查：R22-32 gray/pillow 91×44×86 同样被挡（真实抱枕最大已见
    91cm）。上限提至 100：80~100 区间家具仅餐椅（已被 shape 拉黑），
    其余 >100 或 z 过滤；万一误入 take 秒拒零成本。顺带：took 拿起帧
    的感知现也 vol-max 合并进 world（走到场边的视野可暴露遮挡件，
    与确认帧同款；主循环每件后重分类自动入队）。
39. **家具厚度碰撞判定（2026-09-16，用户提议）**：pillow 此前只看最大
    边，现加**最小边（厚度）守卫** `_FURNITURE_MIN_DIM=58`——数据分界：
    餐椅 min=61~80（61×61×80/86×86×80），已确认方枕 min 最高 56
    （R26-33 57×57×56、R25-33 65×63×53），取 58 居中。min≥58 的块状
    物（含 shape=Unknown 的椅子/脚凳）不进 pillow 判定与队列入队，
    与 shape=chair 拉黑双保险。首版阈值 50 被自己的测试抓出误杀
    （min 53/56 的已确认枕），修正为 58——阈值必须有全量数据支撑。
40. **R27 复盘定案（2026-09-16）**：placed 4（32 蓝圆片 10cm→桶、
    34 白 irregular 17cm→桶、33 blue/box 42×56×62→大件先验 pillow→沙发
    [真伪待复盘 took_33]、52 见下）。两修复：
    - **sandal→shoe 词汇直判**：R27-52 blue/sandal 12×29×12 凉鞋，
      词汇缺失致 29cm 落 20~40 空带→确认 pillow(错)→守卫拦成 trash
      (也错)进桶——正解鞋柜。鞋类尺寸恰在空带，只能靠词汇（顺带
      slipper/loafer）。守卫是"最后防线"不是"正确性来源"。
    - **cylinder≥35→pillow 分支补双守卫**：R27-11 大绿植
      （green/cylinder 86×79×101）被老分支标 pillow（101>100 队列
      兜底未入队，纯 cats 污染）——补 max<100 与 min<58。
41. **确认层根因修复：裁剪 bug + shape 词义提示（2026-09-16，用户质疑
    VLM 边缘化引发）**：
    - **根因发现（confirm_ab.py + 视觉核验）**：旧确认裁剪取左面板
      中央 20%~94%，但 move_to_object 不保证朝向——R27-52 凉鞋的
      "确认图"里**根本没有凉鞋**（拍的是沙发+落地灯，画面里的圆柱
      靠枕正是 VLM 说 pillow 的来源）。当晚全部确认错案（鸡腿/甜甜圈/
      切片/灰方枕/凉鞋）同根因：VLM 在给视野中央的随机物体分类。
    - 修复：①`confirm_image` 改**整个左面板**（物体在感知 objects 里
      = 必在 120° 视野内，全幅保证目标在图中）；②prompt C v2：目标
      特征锚定（颜色/形状/尺寸）+ "找到该物体再分类，其他物体忽略"
      + shape 词义提示表（boot/shoe/sandal/slipper/loafer=鞋、
      round/circle/ring/slice=食物、rock/irregular=垃圾、drumstick=
      鸡腿、方块类按尺寸分）+ 尺寸硬约束保留。
    - **A/B 实证（历史五错案 ×3 次）**：旧 0/5；新鸡腿 food×3 ✓、
      灰方枕 pillow×3 ✓、凉鞋 shoe+other×2、切片 other×3（安全弃权）、
      甜甜圈仍 pillow×3（唯一残留，但 ring 已被规则兜住不进确认）。
      失败模式从"自信错类"变为"other 弃权"（走安全兜底不放错）。
    - R29 实机回归：placed 3（黑方块枕大件先验又命中）、cats 干净、
      无歧义零确认、~60s。守卫层（尺寸/厚度）与确认层（VLM）现在
      各司其职：守卫拦不可能，VLM 在不确定区按特征找物。
42. **纸屑档（2026-09-16，R29 用户复盘点案）**：59/61 号 =
    white/irregular **[10,10,0]**（零高度平面物）并排 11cm @茶几东南
    地毯，用户确认为纸屑——**独一档：零高度平面物直判垃圾桶**。三处
    联动：①`_is_point` 语义从"任一维<1"改"三维全退化（max<1）"——
    [10,10,0] 有面积是物品不是标记（[0,0,0] 墙角点仍排除）；②
    `_rule_category` 顶部加 min(dims)≤0 → trash（若实为场景贴花，
    take 秒拒零成本）；③队列入队的 min 过滤改用 `_is_point` 同语义。
    R29 复盘修正：N=5（3 放 + 2 纸屑漏），修复后此类每件 +13 分。
